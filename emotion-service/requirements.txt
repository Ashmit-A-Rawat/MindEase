fastapi
uvicorn
fer
# Pinned: opencv-contrib-python 5.0.0.93 (latest as of this writing) ships
# with an empty cv2/data/ directory — the bundled Haar cascade XML files
# (which `fer` needs for face detection) are simply missing from that
# release. 4.11.0.86 is the last version confirmed to include them.
opencv-contrib-python==4.11.0.86
pillow
numpy
python-multipart
